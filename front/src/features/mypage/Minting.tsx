import styled from 'styled-components';

const Container = styled.div`
  background: red;
  width: 90%;
  height: 100%;
`;

const Minting = () => {
  return <Container />;
};

export default Minting;
